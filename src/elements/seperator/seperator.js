import React from "react";
import style from "./style.css";

const Seperator = ({children, id, title, style}) => (
    <div style={style} className={"as_seperator"}></div>
);

export default Seperator;
